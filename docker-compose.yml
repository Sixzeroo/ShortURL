version: "3"
services:

    webhook:
        image: golang
        volumes:
         - ./:/go/src/ShortURL
        working_dir: /go/src/ShortURL
        ports:
         - "8080:8080"
        command: ./run.sh
